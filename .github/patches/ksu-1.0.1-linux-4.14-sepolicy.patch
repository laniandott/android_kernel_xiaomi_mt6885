--- a/drivers/kernelsu/selinux/sepolicy.c
+++ b/drivers/kernelsu/selinux/sepolicy.c
@@ -524,6 +524,7 @@
 		return false;
 	}
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 7, 0)
 	struct filename_trans_key key;
 	key.ttype = tgt->value;
 	key.tclass = cls->value;
@@ -561,6 +562,50 @@
 
 	db->compat_filename_trans_count++;
 	return ebitmap_set_bit(&trans->stypes, src->value - 1, 1) == 0;
+#else
+	// Linux < 5.7: the full struct filename_trans {stype, ttype, tclass,
+	// name} is the hashtab key, the datum only holds otype
+	struct filename_trans key;
+	struct filename_trans_datum *trans;
+
+	key.stype = src->value;
+	key.ttype = tgt->value;
+	key.tclass = cls->value;
+	key.name = (char *)o;
+
+	trans = (struct filename_trans_datum *)hashtab_search(
+		db->filename_trans, &key);
+	if (trans) {
+		// Duplicate, overwrite existing data and return
+		trans->otype = def->value;
+		return true;
+	}
+
+	trans = (struct filename_trans_datum *)kcalloc(sizeof(*trans), 1,
+						       GFP_ATOMIC);
+	if (!trans) {
+		pr_err("add_filename_trans: alloc datum failed\n");
+		return false;
+	}
+
+	{
+		struct filename_trans *new_key = (struct filename_trans *)
+			kzalloc(sizeof(*new_key), GFP_ATOMIC);
+		if (!new_key) {
+			pr_err("add_filename_trans: alloc key failed\n");
+			return false;
+		}
+		*new_key = key;
+		new_key->name = kstrdup(key.name, GFP_ATOMIC);
+		trans->otype = def->value;
+		hashtab_insert(db->filename_trans, new_key, trans);
+	}
+
+	// security_compute_filename gates the hashtab lookup on this bitmap
+	// (indexed by ttype), so the rule must be registered here
+	ebitmap_set_bit(&db->filename_trans_ttypes, tgt->value - 1, 1);
+	return true;
+#endif
 }
 
 static bool add_genfscon(struct policydb *db, const char *fs_name,
@@ -616,6 +661,7 @@
 		return false;
 	}
 
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 1, 0)
 	struct ebitmap *new_type_attr_map_array =
 		ksu_realloc(db->type_attr_map_array,
 			    value * sizeof(struct ebitmap),
@@ -654,6 +700,104 @@
 
 	db->sym_val_to_name[SYM_TYPES] = new_val_to_name_types;
 	db->sym_val_to_name[SYM_TYPES][value - 1] = key;
+#else
+	// Linux < 5.1: sym_val_to_name, type_val_to_struct_array and
+	// type_attr_map_array are flex_array, which is not extensible, so we
+	// need to create a new bigger one and copy the old data over
+	struct flex_array *new_type_attr_map_array;
+	struct flex_array *new_type_val_to_struct;
+	struct flex_array *new_val_to_name_types;
+	struct flex_array *old_fa;
+	void *old_elem;
+	int j;
+
+	new_type_attr_map_array =
+		flex_array_alloc(sizeof(struct ebitmap), value,
+				 GFP_ATOMIC | __GFP_ZERO);
+	new_type_val_to_struct =
+		flex_array_alloc(sizeof(struct type_datum *), value,
+				 GFP_ATOMIC | __GFP_ZERO);
+	new_val_to_name_types =
+		flex_array_alloc(sizeof(char *), value,
+				 GFP_ATOMIC | __GFP_ZERO);
+
+	if (!new_type_attr_map_array || !new_type_val_to_struct ||
+	    !new_val_to_name_types) {
+		pr_err("add_type: alloc flex_array failed\n");
+		return false;
+	}
+
+	// preallocate so we don't have to worry about the put ever failing
+	if (flex_array_prealloc(new_type_attr_map_array, 0, value,
+				GFP_ATOMIC | __GFP_ZERO) ||
+	    flex_array_prealloc(new_type_val_to_struct, 0, value,
+				GFP_ATOMIC | __GFP_ZERO) ||
+	    flex_array_prealloc(new_val_to_name_types, 0, value,
+				GFP_ATOMIC | __GFP_ZERO)) {
+		pr_err("add_type: prealloc flex_array failed\n");
+		return false;
+	}
+
+	if (db->type_attr_map_array) {
+		for (j = 0; j < db->type_attr_map_array->total_nr_elements;
+		     j++) {
+			old_elem = flex_array_get(db->type_attr_map_array, j);
+			if (old_elem)
+				flex_array_put(new_type_attr_map_array, j,
+					       old_elem,
+					       GFP_ATOMIC | __GFP_ZERO);
+		}
+	}
+	if (db->type_val_to_struct_array) {
+		for (j = 0;
+		     j < db->type_val_to_struct_array->total_nr_elements;
+		     j++) {
+			old_elem = flex_array_get_ptr(
+				db->type_val_to_struct_array, j);
+			if (old_elem)
+				flex_array_put_ptr(new_type_val_to_struct, j,
+						   old_elem,
+						   GFP_ATOMIC | __GFP_ZERO);
+		}
+	}
+	if (db->sym_val_to_name[SYM_TYPES]) {
+		for (j = 0;
+		     j < db->sym_val_to_name[SYM_TYPES]->total_nr_elements;
+		     j++) {
+			old_elem = flex_array_get_ptr(
+				db->sym_val_to_name[SYM_TYPES], j);
+			if (old_elem)
+				flex_array_put_ptr(new_val_to_name_types, j,
+						   old_elem,
+						   GFP_ATOMIC | __GFP_ZERO);
+		}
+	}
+
+	old_fa = db->type_attr_map_array;
+	db->type_attr_map_array = new_type_attr_map_array;
+	if (old_fa) {
+		flex_array_free(old_fa);
+	}
+	ebitmap_init(flex_array_get(db->type_attr_map_array, value - 1));
+	ebitmap_set_bit(flex_array_get(db->type_attr_map_array, value - 1),
+			value - 1, 1);
+
+	old_fa = db->type_val_to_struct_array;
+	db->type_val_to_struct_array = new_type_val_to_struct;
+	if (old_fa) {
+		flex_array_free(old_fa);
+	}
+	flex_array_put_ptr(db->type_val_to_struct_array, value - 1, type,
+			   GFP_ATOMIC | __GFP_ZERO);
+
+	old_fa = db->sym_val_to_name[SYM_TYPES];
+	db->sym_val_to_name[SYM_TYPES] = new_val_to_name_types;
+	if (old_fa) {
+		flex_array_free(old_fa);
+	}
+	flex_array_put_ptr(db->sym_val_to_name[SYM_TYPES], value - 1, key,
+			   GFP_ATOMIC | __GFP_ZERO);
+#endif
 
 	int i;
 	for (i = 0; i < db->p_roles.nprim; ++i) {
@@ -696,7 +840,12 @@
 static void add_typeattribute_raw(struct policydb *db, struct type_datum *type,
 				  struct type_datum *attr)
 {
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 1, 0)
 	struct ebitmap *sattr = &db->type_attr_map_array[type->value - 1];
+#else
+	struct ebitmap *sattr =
+		flex_array_get(db->type_attr_map_array, type->value - 1);
+#endif
 	ebitmap_set_bit(sattr, attr->value - 1, 1);
 
 	struct hashtab_node *node;

--- a/drivers/kernelsu/selinux/rules.c
+++ b/drivers/kernelsu/selinux/rules.c
@@ -19,8 +19,15 @@
 static struct policydb *get_policydb(void)
 {
 	struct policydb *db;
+#if LINUX_VERSION_CODE >= KERNEL_VERSION(5, 4, 0)
 	struct selinux_policy *policy = rcu_dereference(selinux_state.policy);
 	db = &policy->policydb;
+#else
+	/* Linux < 5.4: no struct selinux_policy / selinux_state.policy.
+	 * Older kernels keep the policydb inside struct selinux_ss
+	 * (security/selinux/ss/services.h); cezanne's backported 4.14 does. */
+	db = &selinux_state.ss->policydb;
+#endif
 	return db;
 }
 
